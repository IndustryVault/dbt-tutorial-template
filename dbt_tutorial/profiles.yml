# replace "my_project" with the name that matches the project name in step 6.
dbt_tutorial:
  outputs:
    dev:
      type: duckdb
      schema: main
      path: md:my_db
      threads: 1
    local:
      type: duckdb
      schema: main
      path: local.duckdb
      threads: 1
  target: dev